<%= render @post %>

<div>
  <%= link_to "Edit this post", edit_post_path(@post) %> |s
  <%= link_to "Back to posts", posts_path %>

  <%= button_to "Destroy this post", @post, method: :delete %>
</div>
<div>
  <h3>Comments</h3>
  <div>
    <%= render 'comments/form', post: @po
    
  st, comment: Comment.new%>
  </div>
  <p><%= pluralize (@comments.count, 'comment') %></p>
  <%= render @comments %>
</div>
